services:
  web:
    build: .
    container_name: cashevide-fastapi-web
    env_file:
      - .env
    networks:
      - proxy-network
      - cashevide_default
    restart: unless-stopped

networks:
  proxy-network:
    external: true
  cashevide_default:
    external: true
